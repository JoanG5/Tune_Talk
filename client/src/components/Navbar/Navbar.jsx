import React from "react";
import { AppBar, Toolbar, Box } from "@mui/material";

export const Navbar = () => {
  return (
    <Box height={"100vh"}>
      <AppBar>
        <Toolbar></Toolbar>
      </AppBar>
    </Box>
  );
};
